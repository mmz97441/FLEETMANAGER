# Copy to functions/.env.<project-id> locally; never commit real values.
APP_URL=https://your-app.example
# Google Cloud project with Route Optimization enabled and IAM configured.
GMPRO_PROJECT_ID=your-google-cloud-project-id
# Optional AI advisor: both values are required, stored only on the server.
GEMINI_API_KEY=
GEMINI_MODEL=
